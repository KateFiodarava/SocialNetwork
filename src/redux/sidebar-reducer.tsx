import {ActionsTypes, StatePropsType} from "./store";

type SidebarStatesPropsType={}

const initialState = {}

export const sidebarReducer = (state: SidebarStatesPropsType = initialState, action: ActionsTypes) => {

    return state;
}

